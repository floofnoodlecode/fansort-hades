{% assign props = item.props %}

{% if props.thumb %}
	{% assign thumb = props.thumb %}
{% else %}
	{% assign thumb = props.img %}
{% endif %}

{
	"src": {{thumb.src}},
	"width": {{thumb.width | divided_by: 1.5}},
	"height": {{thumb.height | divided_by: 1.5}},
	"pixelated": false
}
